# SecondMe Avatar Client

A futuristic, glassmorphism-styled Next.js application that serves as the interface for your **SecondMe Avatar**. This project integrates with the SecondMe API to provide chat, memory management, and personality visualization.

![Dashboard Preview](https://via.placeholder.com/800x400?text=Dashboard+Preview)

## 🚀 Features

### Core
-   **Next.js 15+ App Router**: Built on the latest React framework for performance and scalability.
-   **Visual Excellence**: Custom-built Glassmorphism design system using Tailwind CSS.
-   **Bento Grid Dashboard**: A modern, modular home screen.

### The Personality Suite (Phase 5)
-   **🧠 User Shades**: Visualize your avatar's personality traits and confidence levels (`/shades`).
-   **🗣️ Voice (TTS)**: Text-to-Speech integration allows your avatar to speak its responses in the chat.
-   **💾 Active Memory**: comprehensive memory management system. View your avatar's "Soft Memories" and manually teach it new facts via the "Add Memory" feature.

## 🛠️ Tech Stack

-   **Framework**: Next.js 16 (TypeScript)
-   **Styling**: Tailwind CSS, Framer Motion, Lucide React
-   **API**: SecondMe API (Proxied via Next.js API Routes)
-   **Linting**: ESLint, TypeScript

## 📦 Getting Started

1.  **Clone the repository**:
    ```bash
    git clone <your-repo-url>
    cd <your-project-folder>
    ```

2.  **Install dependencies**:
    ```bash
    npm install
    ```

3.  **Environment Setup**:
    Create a `.env` file in the root directory:
    ```env
    SECONDME_CLIENT_ID=your_client_id
    SECONDME_CLIENT_SECRET=your_client_secret
    ```

4.  **Run Development Server**:
    ```bash
    npm run dev
    ```
    Open [http://localhost:3000](http://localhost:3000) to view the app.

## 🚀 Deployment

This project is optimized for deployment on **Vercel**.

1.  Push your code to a Git repository (GitHub, GitLab, etc.).
2.  Import the project into Vercel.
3.  Add the `SECONDME_CLIENT_ID` and `SECONDME_CLIENT_SECRET` environment variables in the Vercel dashboard.
4.  Deploy!

## 📂 Project Structure

-   `/src/app`: App Router pages and API routes.
-   `/src/components`: Reusable UI components (SideNav, ChatInterface, MemoryCard, etc.).
-   `/src/lib`: Utilities and helper functions.

---

*Generated by Antigravity*
